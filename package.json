{
  "name": "kompact-cli",
  "version": "1.0.0",
  "description": "A cli for generate code base and project using kompact",
  "main": "dist/index.js",
  "bin": {
    "kompact": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "yarn build && kompact create a",
    "dev": "tsc -w",
    "link": "yarn unlink --global kompact-cli && yarn link --global kompact-cli",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "commander": "^12.1.0"
  }
}
